pipeline {
    agent none
    stages {
        stage('build') {
            steps {
                sh 'echo "ponygg"'
                sh '''
                   echo "nicejob"
                   ls -lah
                '''
            }
        }
    }
}
